import { Box } from '@mui/material';
import React from 'react';

const ProductsList = () => {
  return (
    <Box flex={2.5} bgcolor="yellow">
      ProductsList
    </Box>
  );
};

export default ProductsList;
